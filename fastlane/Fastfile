fastlane_version "1.85.0"

default_platform :ios

platform :ios do

  desc "Run all the tests"
  lane :test do
    scan({ 
    	scheme: "PetrMazepa", 
    	device: "iPhone 6s (9.2)" 
    	})
  end
end